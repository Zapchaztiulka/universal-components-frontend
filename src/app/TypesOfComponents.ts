enum TypesOfComponents {
    inputs = "inputs",
    icons = "icons",
    loader = "loader",
    notifications = "notifications",
    selects = "selects",
    tooltips = "tooltips"
}

export default TypesOfComponents;