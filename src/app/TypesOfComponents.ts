enum TypesOfComponents {
    inputs = "inputs",
    icons = "icons",
    loader = "loader",
    notifications = "notifications",
    selects = "selects",
    tooltips = "tooltips",
    modals = "modals",
}

export default TypesOfComponents;