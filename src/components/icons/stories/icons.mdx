# icons

import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import * as Icons from '../.';

<Meta title="Icons/Iconography" />

<IconGallery>
{
   Object.entries(Icons).filter(([name]) => name != 'IconButton').map(([name, Icon]) => (
    <IconItem name={name}>
        <Icon />
    </IconItem>
   ))
}
</IconGallery>